# Playwright / denetim geçici çıktıları
.playwright-mcp/
*.png
!site/assets/img/**/*.png
!_audit/**/*.png

# Denetim ara dosyaları (üretilebilir — _audit/*.mjs betikleriyle yeniden oluşturulur)
# _audit/orijinal/ ARTIK İZLENİYOR — canlı afloday.com'un 34 sayfalık dökümü.
# İçerik doğrulamasının ikinci kaynağı (belge + bu döküm). Başka bilgisayarda
# çalışılabilmesi için depoda durmalı; 296 KB, tamamı düz metin.
_audit/govde/
_audit/probe/
_audit/frames/
_audit/q/
_audit/tmp/
_audit/scan/
_audit/scan2/
_audit/first/
_audit/ai/
_audit/frames*/
_audit/*.jpeg
_audit/*.jpg
_audit/orijinal-gorseller.json
_audit/gorsel-boyut.json
_audit/rapor/

# Eski taslaklar
_arsiv/

# Ortam değişkenleri — gerçek değerler depoya girmez, .env.example örnektir
.env
.env.local
.env*.local

# Sistem
Thumbs.db
.DS_Store
node_modules/

# Keşif sırasında kullanılan tek seferlik görüntüleyiciler (üretimde işlevi yok)
_audit/*.html
_audit/cands.json
_audit/ogsec/

# Teklif belgesi — fiyat ve ticari koşullar içerir, depoya girmez
_teklif/

# Next.js
node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# Varlıklar site/assets'te kaynak olarak duruyor; public/ altına derleme
# öncesi kopyalanıyor (bkz. _build/varliklar.mjs). Git'te iki kopya tutmuyoruz.
public/

# Claude Code makine-yerel ayarları — izin kayıtları oturuma özel, depoya girmez
.claude/

# Laboratuvar geçici çıktıları (tasarım v2 çalışması)
_lab/cikti/
